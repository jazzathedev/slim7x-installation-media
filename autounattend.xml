<?xml version="1.0" encoding="utf-8"?>
<unattend xmlns="urn:schemas-microsoft-com:unattend"
          xmlns:wcm="http://schemas.microsoft.com/WMIConfig/2002/State">

  <!--
    Windows 11 Pro ARM64 - unattended tweaks (Rufus-style)
    - "I don't have internet" button appears in OOBE  → local account
    - Microsoft account screen hidden                 → local account
    - EULA auto-accepted
    - Telemetry set to minimum (Security only)
    - Advertising ID disabled
    - Suggested content / tips disabled
    - Most OOBE screens skipped
  -->

  <settings pass="specialize">
    <component name="Microsoft-Windows-Shell-Setup"
               processorArchitecture="arm64"
               publicKeyToken="31bf3856ad364e35"
               language="neutral" versionScope="nonSxS">
      <RunSynchronous>

        <!-- 1. BypassNRO: makes "I don't have internet" appear in OOBE network screen -->
        <RunSynchronousCommand wcm:action="add">
          <Order>1</Order>
          <Path>reg add HKLM\SOFTWARE\Microsoft\Windows\CurrentVersion\OOBE /v BypassNRO /t REG_DWORD /d 1 /f</Path>
        </RunSynchronousCommand>

        <!-- 2. Telemetry: 0 = Security only (minimum), requires Pro/Enterprise -->
        <RunSynchronousCommand wcm:action="add">
          <Order>2</Order>
          <Path>reg add HKLM\SOFTWARE\Policies\Microsoft\Windows\DataCollection /v AllowTelemetry /t REG_DWORD /d 0 /f</Path>
        </RunSynchronousCommand>

        <!-- 3. Disable advertising / personalised ads ID -->
        <RunSynchronousCommand wcm:action="add">
          <Order>3</Order>
          <Path>reg add HKLM\SOFTWARE\Microsoft\Windows\CurrentVersion\AdvertisingInfo /v Enabled /t REG_DWORD /d 0 /f</Path>
        </RunSynchronousCommand>

        <!-- 4. Disable "Get tips, tricks and suggestions" -->
        <RunSynchronousCommand wcm:action="add">
          <Order>4</Order>
          <Path>reg add HKLM\SOFTWARE\Policies\Microsoft\Windows\CloudContent /v DisableSoftLanding /t REG_DWORD /d 1 /f</Path>
        </RunSynchronousCommand>

        <!-- 5. Disable Cortana in OOBE -->
        <RunSynchronousCommand wcm:action="add">
          <Order>5</Order>
          <Path>reg add HKLM\SOFTWARE\Policies\Microsoft\Windows\Windows Search /v AllowCortana /t REG_DWORD /d 0 /f</Path>
        </RunSynchronousCommand>

      </RunSynchronous>
    </component>
  </settings>

  <settings pass="oobeSystem">
    <component name="Microsoft-Windows-Shell-Setup"
               processorArchitecture="arm64"
               publicKeyToken="31bf3856ad364e35"
               language="neutral" versionScope="nonSxS">
      <OOBE>
        <!-- Auto-accept EULA -->
        <HideEULAPage>true</HideEULAPage>
        <!-- Skip OEM registration page -->
        <HideOEMRegistrationScreen>true</HideOEMRegistrationScreen>
        <!-- Hide Microsoft account screen - forces local account path (Pro only) -->
        <HideOnlineAccountScreens>true</HideOnlineAccountScreens>
        <!-- Skip wireless setup (we have no wifi driver yet anyway) -->
        <HideWirelessSetupInOOBE>true</HideWirelessSetupInOOBE>
        <!-- Don't auto-enable recommended privacy settings -->
        <ProtectYourPC>3</ProtectYourPC>
      </OOBE>
    </component>
  </settings>

</unattend>
